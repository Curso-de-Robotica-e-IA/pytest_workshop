[project]
name = "thiago-jorge-banco"
version = "0.1.0"
description = ""
authors = [
    {name = "Thiago Lins",email = "thiago.jorge2001@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
]

[tool.poetry]
packages = [{include = "thiago_jorge_banco", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.1.1,<10.0.0)",
    "pytest-cov (>=7.1.0,<8.0.0)",
    "pytest-mock (>=3.15.1,<4.0.0)",
    "requests (>=2.34.2,<3.0.0)"
]
