[project]
name = "joao-maranhao-estufa-agrotech"
version = "0.1.0"
description = ""
authors = [
    {name = "joaopmarinho",email = "jpamm@softex.cin.ufpe.br"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
]

[tool.poetry]
packages = [{include = "joao_maranhao_estufa_agrotech", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.1.1,<10.0.0)",
    "pytest-cov (>=7.1.0,<8.0.0)",
    "pytest-mock (>=3.15.1,<4.0.0)"
]
