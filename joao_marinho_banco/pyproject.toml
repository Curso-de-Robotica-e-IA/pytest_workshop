[project]
name = "joao-marinho-banco"
version = "0.1.0"
description = ""
authors = [
    {name = "joaopmarinho",email = "jpamm@softex.cin.ufpe.br"}
]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests (>=2.34.2,<3.0.0)",
    "pytest (>=9.1.1,<10.0.0)"
]

[tool.poetry]
packages = [{include = "joao_marinho_banco", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
