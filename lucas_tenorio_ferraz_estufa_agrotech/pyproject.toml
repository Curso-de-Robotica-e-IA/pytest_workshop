[project]
name = "lucas-tenorio-ferraz-estufa-agrotech"
version = "0.1.0"
description = ""
authors = [
    {name = "Lucas Tenorio",email = "ltf@softex.cin.ufpe.br"}
]
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
]

[tool.poetry]
packages = [{include = "lucas_tenorio_ferraz_estufa_agrotech", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pytest (>=9.1.1,<10.0.0)",
    "pytest-cov (>=7.1.0,<8.0.0)",
    "pytest-mock (>=3.15.1,<4.0.0)"
]
